"""
neet model package feature transforms/engineering

"""

import math
import numpy as np
import pandas as pd

# Example -- to be changed #
def transform_time_features(X: pd.DataFrame) -> pd.DataFrame:

    result = # pd.DataFrame()
    return result

# Example -- to be changed #
def transform_XYZ_features(X: pd.DataFrame) -> pd.DataFrame:

    result = # pd.DataFrame()
    return result
